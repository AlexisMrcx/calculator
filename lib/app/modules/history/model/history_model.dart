class History {
  History({this.title, this.dateTime, this.result, this.isChecked, this.id});

  final String title;
  final DateTime dateTime;
  final double result;
  final int id;
  bool isChecked;
  
}